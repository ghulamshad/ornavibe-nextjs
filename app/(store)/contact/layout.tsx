import { ReactNode } from 'react';

export const metadata = {
  title: 'Contact | Ornavibe',
  description: 'Contact Ornavibe — questions about your order or our gift baskets. Reach out to Rason Business.',
};

export default function ContactLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
